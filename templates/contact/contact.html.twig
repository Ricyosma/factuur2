{%  extends 'base.html.twig' %}
{% block body %}
    <div>
        {{ form_start(contact_form) }}
        {{ form_widget(contact_form) }}
        <input type="submit" value="Verzend" class="btn btn-success">
        {{ form_end(contact_form) }}
    </div>

{%  endblock %}